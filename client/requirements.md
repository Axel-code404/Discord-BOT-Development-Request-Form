## Packages
framer-motion | Complex animations for the space background and UI transitions
lucide-react | Icons for the UI
date-fns | Formatting dates in chat
clsx | Conditional class names
tailwind-merge | Merging tailwind classes intelligently

## Notes
The app requires a dynamic starfield background.
Authentication is handled via Replit Auth (already integrated).
Chat interface needs to handle real-time-like updates (using TanStack Query polling for MVP or invalidateQueries on focus).
Admin route is `/admin-page`.
